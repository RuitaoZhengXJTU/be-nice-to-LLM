To solve the given nonlinear programming problem, we can outline the steps involved in formulating and solving it. However, as a text-based AI, I cannot directly execute code or perform numerical optimization. Instead, I can guide you on how to implement this using a programming language like Python with libraries such as `scipy` or `cvxpy`.

Here's how you can set up and solve the problem using Python and `scipy.optimize`:

```python
import numpy as np
from scipy.optimize import minimize, LinearConstraint

# Define the number of variables and constraints
num_vars = 100
num_constraints = 30

# Objective function: minimize sum(x[i]^2 for i in 1..100)
def objective(x):
    return np.sum(x**2)

# Constraints: sum(x[i] for i in (3*j-2)..(3*j)) >= j * 0.1 for j in 1..30
def constraint_func(x):
    return np.array([np.sum(x[3*j-3:3*j]) - j * 0.1 for j in range(1, num_constraints + 1)])

# Initial guess
x0 = np.zeros(num_vars)

# Bounds for each variable
bounds = [(-10, 10) for _ in range(num_vars)]

# Define the constraints in the form required by scipy
linear_constraint = LinearConstraint(np.eye(num_vars), 0, np.inf)
constraints = {'type': 'ineq', 'fun': constraint_func}

# Solve the optimization problem
result = minimize(objective, x0, method='trust-constr', bounds=bounds, constraints=constraints)

# Prepare the result in the desired JSON format
solution = {
    "x": result.x.tolist(),
    "objective_value": result.fun
}

import json
print(json.dumps(solution, indent=4))
```

### Explanation:
1. **Objective Function**: The function `objective` computes the sum of squares of the variables.
2. **Constraints**: The function `constraint_func` generates the constraints based on the problem statement.
3. **Initial Guess**: We start with an initial guess of zeros for all variables.
4. **Bounds**: Each variable is bounded between -10 and 10.
5. **Optimization**: We use the `minimize` function from `scipy.optimize` to find the optimal values of `x`.
6. **Output**: Finally, we format the results as a JSON object.

### Note:
Make sure you have the required libraries installed. You can install them using pip if you haven't done so:

```bash
pip install numpy scipy
```

This code will give you the optimal values of `x` and the optimal objective value in the specified JSON format. You can run this code in your local Python environment.