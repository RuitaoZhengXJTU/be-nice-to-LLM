Solve the nonlinear programming problem below. {{N_VARS}} variables, {{N_CONSTRAINTS}} constraints. Return the solution as JSON with keys "x" (list of {{N_VARS}} floats) and "objective_value" (float). No explanation—output only the JSON.

Problem:
{{PROBLEM_SPEC}}
